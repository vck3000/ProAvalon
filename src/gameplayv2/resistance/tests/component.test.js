import { Resistance } from '../entities/resistance'

test('properly initialize the Spy entity', () =>{
    //do test case here..
});

test('properly initialize the Resistance entity', () =>{
    //do test case here..
    //example:
    expect(1+2).toBe(3);

    //or for an array:
    const arraychar = ['a', 'b']
    const array = [1, 2]
    //this will throw an Error
    expect(cloneArray(array)).toEqual(arraychar);
});

//take a look at rewards/tests/getRewards.test.ts
//if you want to run this, do npm test on terminal