const constants = require('../constants');

obj = {
    key: constants.TIER2_BADGE,
    adminReq: false,
    modReq: false,
    gamesPlayedReq: 0,
    donationReq: constants.tier2_donation,
};

module.exports = obj;
