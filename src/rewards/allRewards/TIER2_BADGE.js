import constants from '../constants';

const obj = {
  key: constants.TIER2_BADGE,
  adminReq: false,
  modReq: false,
  gamesPlayedReq: 0,
  donationReq: constants.tier2_donation,
};

export default obj;
