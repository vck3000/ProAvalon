import constants from '../constants';

const obj = {
  key: constants.TIER1_BADGE,
  adminReq: false,
  modReq: false,
  gamesPlayedReq: 0,
  donationReq: constants.tier1_donation,
};

export default obj;
