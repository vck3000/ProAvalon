const constants = require('../constants');

obj = {
    key: constants.TIER1_BADGE,
    adminReq: false,
    modReq: false,
    gamesPlayedReq: 0,
    donationReq: constants.tier1_donation,
};

module.exports = obj;
